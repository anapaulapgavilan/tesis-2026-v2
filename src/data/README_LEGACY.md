# LEGACY — Estos scripts se conservan como referencia.
# La versión activa está en `src/tesis_alcaldesas/data/`.
# Para ejecutar el pipeline, usar:
#   python -m tesis_alcaldesas.data.extract_panel
#   python -m tesis_alcaldesas.data.build_features
