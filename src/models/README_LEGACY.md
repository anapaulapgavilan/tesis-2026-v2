# LEGACY — Estos scripts se conservan como referencia.
# La versión activa está en `src/tesis_alcaldesas/models/`.
# Para ejecutar el pipeline, usar:
#   python -m tesis_alcaldesas.models.table1_descriptives
#   python -m tesis_alcaldesas.models.twfe
#   etc.
