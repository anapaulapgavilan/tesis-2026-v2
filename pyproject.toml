[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tesis-alcaldesas"
version = "0.1.0"
description = "Alcaldesas e inclusión financiera — tesis de grado"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
